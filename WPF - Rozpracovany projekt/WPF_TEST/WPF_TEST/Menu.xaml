<Page x:Class="WPF_TEST.Menu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_TEST"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Menu">
    
    
    <Grid Background="White">
        <!-- Definice dvou řádků: horní proužek + obsah -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- adaptivní výška pro header -->
            <RowDefinition />
            <!-- zbývající prostor pro mřížku -->
        </Grid.RowDefinitions>

        <!-- Horní proužek s klikacím textem -->
        <Border Background="LightGray" Padding="10" Grid.Row="0">
        </Border>

        <Button Content="Účet" Grid.Row="0" Click="BtnPrepnutiNaUcet" HorizontalAlignment="Right" Height="41" Margin="712,0,0,0" VerticalAlignment="Center" Width="88" FontSize="16" FontWeight="Bold"/>

        <Grid Grid.Row="1" Margin="20">
            <ContentControl x:Name="MainContent" />
        </Grid>

        <Button Content="Výpis knih" HorizontalAlignment="Left" Height="41" VerticalAlignment="Center" Width="102" FontWeight="Bold" FontSize="16" Click="BtnVypisKnih"/>

    </Grid>
</Page>
